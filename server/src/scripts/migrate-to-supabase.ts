// Script de migração temporariamente desabilitado
// A tabela users não está definida no schema do Drizzle

console.log('🚀 Script de migração desabilitado - tabela users não definida');

// async function migrateRealUsers() {
//   // Conteúdo comentado
// }

// Executar migração
// migrateRealUsers().then(() => {
//   console.log('🏁 Script de migração finalizado');
//   process.exit(0);
// }).catch((error) => {
//   console.error('💥 Erro fatal:', error);
//   process.exit(1);
// }); 